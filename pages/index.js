import { FormData } from "@/components/form/feedback-form";
import React from "react";

const Home = () => {
  return (
    <main className="flex items-center justify-center  h-[100vh]">
      <FormData />
    </main>
  );
};

export default Home;
